{
	"name": "prismic-schema-generator",
	"version": "1.0.0",
	"description": "Generates a gql SDL file from prismic",
	"main": "src/index.ts",
	"author": "Max Smouha",
	"license": "MIT",
	"keywords": [
		"prismic",
		"schema",
		"schema generator"
	],
	"publishConfig": {
		"access": "public"
	},
	"bin": {
		"prismic-schema": "dist/index.js"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "rimraf dist && tsc"
	},
	"devDependencies": {
		"@types/commander": "^2.12.2",
		"@types/isomorphic-fetch": "^0.0.35",
		"@types/node": "^13.7.7",
		"typescript": "^3.8.3"
	},
	"dependencies": {
		"commander": "^4.1.1",
		"graphql": "^14.6.0",
		"isomorphic-fetch": "^2.2.1",
		"rimraf": "^3.0.2"
	}
}
